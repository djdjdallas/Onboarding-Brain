/**
 * Dashboard. Placeholder for Step 3 (auth shell) — the real dealer list with
 * open-findings counts lands in Step 5.
 */
export default function DashboardPage() {
  return (
    <div className="space-y-2">
      <h1 className="text-2xl font-semibold tracking-tight">Dashboard</h1>
      <p className="text-muted-foreground">
        You&apos;re signed in. The dealer list and audit-finding counts land in
        Step 5.
      </p>
    </div>
  )
}
